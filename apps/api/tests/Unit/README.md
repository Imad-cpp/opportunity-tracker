# Unit tests

Focused unit tests belong here when domain policies are extracted into independently testable units. The current identity step is covered by application-level feature tests instead of placeholder assertions.
